spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/fastsns"
    username: REDIS_NAME
    password: REDIS_PASSWORD

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  jmx:
    enabled: false
